{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/webhook", "dest": "/api/webhook.js" },
    { "src": "/api/tick", "dest": "/api/tick.js" }
  ],
  "crons": [
    {
      "path": "/api/tick",
      "schedule": "*/5 * * * *"
    }
  ]
}
